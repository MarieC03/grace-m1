# LBM shadow at cluster resolution: 192^3 cells over +-0.5, dx = 0.0052.  A beam is cast
# past an opaque sphere; the geometric shadow behind it must stay dark.
# Check: scripts/lbm_radial_profile.py FILE shadow 0.1 0.4 -- the core of the shadow was
# 40-50x dimmer than the rim at dx = 1/128.  The residual filling is the same ~10 degree
# beam cone as in the crossed-beam test, not spatial diffusion.
# CLUSTER SIZE, one MI300A node (4 ranks): 512 quadrants of 24^3 cells.
# 24^3 blocks with 4 ghost zones are 32^3 padded, 367 MB per quadrant, so the run
# holds ~188 GB of the node's 512 GB.  12^3 blocks would cost 2.4x that per useful
# cell.  To go finer use 32^3 blocks (717 MB per quadrant, ~367 GB) at the same level.
# Runtime: ~2 min (collisions included).
# Build: -DGRACE_RADIATION_SCHEME=LBM -DGRACE_M1_NU_SPECIES=1 -DGRACE_METRIC_EVOL=COWLING
name: "lbm_shadow_hr"
amr: {
  npoints_block_x: 24,
  npoints_block_y: 24,
  npoints_block_z: 24,
  xmin: -0.5, xmax: 0.5,
  ymin: -0.5, ymax: 0.5,
  zmin: -0.5, zmax: 0.5,
  n_ghostzones: 4,
  initial_refinement_level: 3,
  max_refinement_level: 3,
  regrid_every: -1,
  regrid_at_postinitial: false,
  reflection_symmetries: {x: false, y: false, z: false}
}
IO: {
  info_output_every: 16,
  info_output_max_reductions: ["Erad1"],
  scalar_output_every: 16,
  scalar_output_minmax: ["Erad1"],
  scalar_output_integral: ["Erad1"],
  volume_output_every: 40,
  plane_surface_output_every: 16,
  plane_surface_output_cell_variables: ["Erad1"],
  sphere_surface_output_every: -1,
  diagnostic_output_every: -1,
  volume_output_cell_variables: ["Erad1"],
  output_extra_quantities: false
}
system: {
  console_log_level: "info",
  file_log_level: "info",
  flush_logs_based_on_severity: false,
  n_device_streams: 1
}
evolution: {
  time_stepper: "rk3",
  cfl_factor: 0.9,
  force_unit_wavespeed: true,
  final_iteration: 160,
  termination_condition: "iteration"
}
grmhd: {
  id_type: "minkowski_vacuum",
  vacuum: {
    rho_floor: 1e-20, press_floor: 1e-30,
    velocity_x: 0., velocity_y: 0., velocity_z: 0.
  },
  atmosphere: { ye_fl: 0, temp_fl: 0, rho_fl: 1e-50, rho_scaling: 0, temp_scaling: 0 }
}
eos: {
  eos_type: "hybrid",
  hybrid_eos: { cold_eos_type: "piecewise_polytrope", gamma_th: 1.8 },
  eps_maximum: 1e+05,
  piecewise_polytrope: { n_pieces: 1, gammas: [ 2. ], rhos: [ ], kappa_0: 1.0 }
}
m1: {
  id_type: "shadow",
  eas: { kinds: ["test"] },
  backreaction: { enabled: false }
}
lbm: {
  bc_kind: "none"
}
checkpoints: { checkpoint_at_termination: false }
